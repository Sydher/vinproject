{% extends 'admin/admin_base.html.twig' %}

{% block title %}Modifier une appellation{% endblock %}

{% block body %}
    <div class="card mx-auto medium_form">
        <div class="card-body">
            <h5 class="card-title">Modifier l'appellation {{ appellation.name }}</h5>
            {{ form_start(form) }}
            <div class="row">
                <div class="col">
                    {{ form_row(form.name) }}
                </div>
                <div class="col">
                    {{ form_row(form.region) }}
                </div>
            </div>
            {{ form_row(form.description) }}

            <div class="text-center">
                <a class="btn btn-light" href="{{ path('admin_shop_list_appellation') }}">
                    Annuler
                </a>
                <button class="btn btn-success" type="submit">
                    Modifier
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
