{% extends 'base.html.twig' %}

{% block title %}{{ region.name }}{% endblock %}
{% block description %}
    <meta name="description" content="Tous les vins de la région {{ region.name }}">
{% endblock %}

{% block body %}
    <div class="container mt-3 mb-3">
        {{ include('components/titles/title1.html.twig', { title: region.name }) }}
        <div>
            {% for appellation in region.appellations %}
                {{ include('shop/wine/_appellation_list_item.html.twig',
                    {appellation: appellation}) }}
            {% endfor %}
        </div>
    </div>
{% endblock %}
